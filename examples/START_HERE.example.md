# Start here

> Generated by [start-here](https://github.com/minoruthenextkami-afk/start-here) · 2026-09-23

## What this is
Minimal Express API that serves job listings and writes an append-only application log.

## Run it

| Step | Command |
|------|---------|
| Install | `npm ci` |
| Dev | `npm run dev` |
| Test | `npm test` |
| Build | `npm run build` |

Stack: Node.js · Express · SQLite · Vitest

## Where to change what

| I want to… | Open |
|------------|------|
| Add/change API route | `src/routes/listings.js` (+ test `test/listings.test.js`) |
| Change data/schema | `src/db/schema.sql` and `migrations/` |
| Change UI (if any) | `public/index.html` |
| Fix auth bug | `src/middleware/auth.js` + `test/auth.test.js` |
| Add a test | `test/*.test.js` (Vitest) |
| Config / env | `.env.example`, `src/config.js` |
| Deploy | `Dockerfile`, `.github/workflows/ci.yml` |
| Local dev loop | `npm run dev` (hot reload) → `npm test` |

## First PR (suggested)
Run `npm test` green, then fix the failing liveness case in `scripts/check-liveness.mjs` flagged in issue #12 — one file, covered by `test/liveness.test.js`.

## Gotchas
- Port 3000 must be free; override with `PORT=`
- Do not edit `generated/` — produced by `npm run codegen`
- Workspace root uses npm workspaces; run scripts from package dir

## Layout
- `src/` — API and domain code
- `test/` — Vitest suites
- `migrations/` — SQL migrations
- `scripts/` — maintainer CLI scripts
- `.github/workflows/` — CI (test + lint)
